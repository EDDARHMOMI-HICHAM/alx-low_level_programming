/**
 * _strspn - calculates the length of the initial segment of s which consists entirely of bytes from accept.
 * @s: pointer to a string
 * @accept: pointer to a string containing the characters to match
 *
 * Return: number of bytes from the initial segment of s that consist of bytes from accept
 */
unsigned int _strspn(char *s, char *accept)
{
return (0);
}
