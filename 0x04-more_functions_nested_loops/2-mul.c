#include <stdio.h>

/**
 * mul - multiplies two integers
 *
 * @a: first number
 *
 * @b: second nember
 *
 * Return: the resolt of a*b
 */
int mul(int a, int b)
{
int result;

result = a * b;

return (result);
}
